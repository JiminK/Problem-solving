#include <iostream>
using namespace std;

#define MAX_N 1000

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n; cin >> n;
    int arrA[MAX_N + 1];
    // for (int i = 0; i < )

    return 0;
}