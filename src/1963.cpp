/*
정점: "비밀번호 4자리 숫자의 상태(소수인지)"
간선: 두 상태 사이에서 이동한 개념 -> "숫자를 바꾸는 행위"

시작정점은? -> 

인접리스트 필요 없다. (인접리스트 없이도 인접한 정점 빠르게 찾을 수 있기 때문)

가중치 1로 다 똑같으니까 BFS 사용 가능!
*/
#include <iostream>

using namespace std;

void getInput() {
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    getInput();

    return 0;
}